import React from 'react';
import ReactDOM from 'react-dom/client';
import CsvToPdf from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <CsvToPdf />
  </React.StrictMode>
);
